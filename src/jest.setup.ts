// This file is intentionally left blank for now to avoid module resolution issues with @types/jest.
// Jest's global types should still be available if correctly configured in tsconfig.json or via auto-detection.
